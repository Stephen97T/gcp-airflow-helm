apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: airflow-worker-role
  namespace: {{ .Values.namespace.name }}
rules:
  - apiGroups: [""]
    resources: ["pods", "pods/log", "pods/exec","events","pods/status"]
    verbs: ["create", "get", "list", "watch", "delete", "patch"]